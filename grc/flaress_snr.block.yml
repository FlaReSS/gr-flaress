# auto-generated by grc.converter

id: flaress_snr
label: snr
category: '[flaress]'

parameters:
-   id: carrier
    label: Measurement
    dtype: enum
    default: 'True'
    options: ['True', 'False']
    option_labels: [Carrier to Noise Ratio, Signal to Noise Ratio]
-   id: auto_carrier
    label: Carrier self-dection
    dtype: enum
    default: 'True'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: freq_central
    label: Central Frequency
    dtype: real
    default: '0'
-   id: samp_rate
    label: sample rate
    dtype: real
    default: samp_rate
-   id: nintems
    label: FFT size
    dtype: int
    default: '1024'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: signal_bw
    label: Signal bandwidth
    dtype: int
-   id: noise_bw
    label: Noise bandwidth
    dtype: int

inputs:
-   domain: stream
    dtype: float
    vlen: ${ nintems }

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import flaress
    make: flaress.snr(${auto_carrier}, ${carrier}, ${freq_central}, ${samp_rate},
        ${nintems}, ${signal_bw}, ${noise_bw})

file_format: 1
