# auto-generated by grc.converter

id: flaress_null_sink
label: Null Sink
category: '[flaress]'

templates:
    imports: from gnuradio import flaress
    make: flaress.null_sink(${type.size}*${vlen})

parameters:
-   id: type
    label: Input Type
    dtype: enum
    options: [complex, float, int, short, byte, s64, f64]
    option_labels: [Complex, Float, Int, Short, Byte, Int64, Float64]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_int, gr.sizeof_short,
            gr.sizeof_char, flaress.sizeof_long, gr.sizeof_double]
    hide: part
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: num_inputs
    label: Num Inputs
    dtype: int
    default: '1'
    hide: part
-   id: bus_conns
    label: Bus Connections
    dtype: raw
    default: '[[0,],]'
    hide: part

inputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen }
    multiplicity: ${ num_inputs }

asserts:
- ${ num_inputs >= 1 }
- ${ vlen > 0 }

file_format: 1
