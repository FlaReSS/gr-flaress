# auto-generated by grc.converter

id: flaress_vector_source_int64
label: Vector Source Int64
category: '[flaress]'

parameters:
-   id: vector
    label: Vector
    dtype: raw
    default: (0, 0, 0)
-   id: tags
    label: Tags
    dtype: raw
    default: '[]'
-   id: repeat
    label: Repeat
    dtype: enum
    default: 'True'
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }

outputs:
-   domain: stream
    dtype: s64
    vlen: ${ vlen }
asserts:
- ${ vlen > 0 }

templates:
    imports: import flaress
    make: flaress.vector_source_int64(${vector}, ${repeat}, ${vlen}, ${tags})

file_format: 1
