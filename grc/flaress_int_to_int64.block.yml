# auto-generated by grc.converter

id: flaress_int_to_int64
label: Int to Int64
category: '[flaress]'

parameters:
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: scale
    label: Scale
    dtype: s64
    default: '1'

inputs:
-   domain: stream
    dtype: int
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: s64
    vlen: ${ vlen }

templates:
    imports: import flaress
    make: flaress.int_to_int64(${vlen}, ${scale})
    callbacks:
    - set_scale(${scale})

file_format: 1
