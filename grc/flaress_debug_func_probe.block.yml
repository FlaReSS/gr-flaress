# auto-generated by grc.converter

id: flaress_debug_func_probe
label: Debug Function Probe
category: '[flaress]'

templates:
    imports: from gnuradio import flaress
    make: flaress.debug_func_probe(${type.size}*${vlen})

parameters:
-   id: type
    label: IO Type
    dtype: enum
    options: [complex, float, f64, int, s64, short, byte]
    option_labels: [Complex, Float, Float64, Int, Int64, Short, Byte]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_double, gr.sizeof_int,
            flaress.sizeof_long, gr.sizeof_short, gr.sizeof_char]
    hide: part
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen }
    multiplicity: '1'

file_format: 1
