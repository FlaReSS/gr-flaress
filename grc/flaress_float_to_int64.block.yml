# auto-generated by grc.converter

id: flaress_float_to_int64
label: Float To Int64
category: '[flaress]'

parameters:
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: scale
    label: Scale
    dtype: real
    default: '1'

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: s64
    vlen: ${ vlen }

templates:
    imports: import flaress
    make: flaress.float_to_int64(${vlen}, ${scale})
    callbacks:
    - set_scale(${scale})

file_format: 1
