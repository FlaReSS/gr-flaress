# auto-generated by grc.converter

id: flaress_integer_math_xx
label: Integer Math
category: '[flaress]'

parameters:
-   id: type
    label: Input Type
    dtype: enum
    default: int
    options: [int, s64]
    option_labels: [Integer, Integer64]
    option_attributes:
        fcn: [ii, ll]
    hide: part
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: N_int
    label: N of bits integer part
    dtype: int

inputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen }
asserts:
- ${ vlen > 0 }

templates:
    imports: import flaress
    make: flaress.integer_math_${type.fcn}(${vlen}, ${N_int})

file_format: 1
