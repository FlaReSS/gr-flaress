# auto-generated by grc.converter

id: flaress_vector_sink_x
label: Vector Sink
category: '[flaress]'

templates:
    imports: from gnuradio import flaress
    make: flaress.vector_sink_${type.fcn}(${vlen}, ${reserve_items})

parameters:
-   id: type
    label: Input Type
    dtype: enum
    options: [s64, f64]
    option_labels: [Int64, Double]
    option_attributes:
        fcn: [int64, double]
    hide: part
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: reserve_items
    label: Reserve memory for items
    dtype: int
    default: '1024'
    hide: part

inputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen }

asserts:
- ${ vlen > 0 }

file_format: 1
