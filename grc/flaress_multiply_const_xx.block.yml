# auto-generated by grc.converter

id: flaress_multiply_const_double
label: Multiply Const
category: '[flaress]'

parameters:
-   id: type
    label: IO Type
    dtype: enum
    options: [f64, s64]
    option_labels: [Float64, Int64]
    option_attributes:
        fcn: [double, int64]
    hide: part
-   id: const
    label: Constant
    dtype: ${ type }
    default: '0'
-   id: vlen
    label: Vec Length
    dtype: int
    default: '1'
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen }
asserts:
- ${ vlen > 0 }

templates:
    imports: import flaress
    make: flaress.multiply_const_${type.fcn}(${const}, ${vlen})
    callbacks:
    - set_k(${const})

file_format: 1
