# Copyright 2011 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-flaress
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Include python install macros
########################################################################
include(GrPython)
if(NOT PYTHONINTERP_FOUND)
    return()
endif()

add_subdirectory(bindings)

########################################################################
# Install python sources
########################################################################
GR_PYTHON_INSTALL(
    FILES
    __init__.py
#    sine_debug.py
#    snr_estimator_cf.py
#    snr_estimator_cfv.py
    DESTINATION ${GR_PYTHON_DIR}/gnuradio/flaress
)

########################################################################
# Handle the unit tests
########################################################################
if(ENABLE_TESTING)

    include(GrTest)

    set(GR_TEST_TARGET_DEPS gnuradio-flaress)

    # Create a package directory that tests can import. It includes everything
    # from `python/`.
    add_custom_target(
      copy_module_for_tests ALL
      COMMAND ${CMAKE_COMMAND} -E copy_directory ${CMAKE_CURRENT_SOURCE_DIR}
              ${CMAKE_BINARY_DIR}/test_modules/gnuradio/flaress/
    )

    file(GLOB py_qa_test_files "qa_*.py")

    if(NOT SNDFILE_FOUND)
        list(REMOVE_ITEM py_qa_test_files ${CMAKE_CURRENT_SOURCE_DIR}/qa_wavfile.py)
    endif()

    foreach(py_qa_test_file ${py_qa_test_files})
        get_filename_component(py_qa_test_name ${py_qa_test_file} NAME_WE)
        if(CMAKE_CROSSCOMPILING)
            get_filename_component(py_qa_test_file ${py_qa_test_file} NAME)
        endif(CMAKE_CROSSCOMPILING)
        gr_add_test(${py_qa_test_name} ${QA_PYTHON_EXECUTABLE} -B ${py_qa_test_file})
    endforeach(py_qa_test_file)

endif(ENABLE_TESTING)
